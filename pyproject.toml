[project]
name = "sftp-watchdog"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "watchdog>=4.0.0",
]

[project.scripts]
sftp-watchdog = "sftp_watchdog.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
