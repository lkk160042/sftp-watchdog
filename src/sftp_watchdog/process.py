from pathlib import Path


def process(file_path: Path) -> None:
    """Replace this function with the real business logic."""
    print(f"[PROCESS] start: {file_path}")

    # Pseudo code for the actual workflow:
    # data = read_csv(file_path)
    # validate(data)
    # save_to_db(data)
    # make_result(data)

    print(f"[PROCESS] done: {file_path}")
